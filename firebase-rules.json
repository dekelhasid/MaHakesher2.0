{
  "rules": {
    ".read": true,
    "games": { ".write": "auth != null && auth.token.email === 'admin@mahakesher2nekuda0.com'" },
    "stats": {
      "$gameId": {
        ".write": "auth != null"
      }
    },
    "players": {
      "$playerName": {
        ".write": "auth != null"
      }
    },
    "puzzles": { ".write": "auth != null && auth.token.email === 'admin@mahakesher2nekuda0.com'" }
  }
}
