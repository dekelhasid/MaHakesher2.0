/*
 * Paste the Firebase Web App configuration here after creating the Firebase project.
 * The config is safe to publish; Firebase Rules protect the data.
 */
window.MAHAKESHER_FIREBASE_CONFIG = null;
